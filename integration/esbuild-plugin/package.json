{
  "name": "@isometry/esbuild-plugin",
  "version": "0.0.1",
  "scripts": {
    "build": "rm -rf dist && esbuild src/index.ts --outfile=dist/main.cjs.js --platform=node --bundle --log-level=warning --external:esbuild",
    "test": ""
  },
  "main": "dist/main.cjs.js",
  "devDependencies": {
    "@isometry/tsconfig": "workspace:*",
    "@types/node": "~18.8.4",
    "esbuild": "~0.15.10",
    "typescript": "~4.8.4"
  }
}
